type: object
description: |
  案件一覧
allOf:
  - $ref: "../MetaListResponse.yaml"
  - type: object
    required:
      - items
    properties:
      items:
        type: array
        items:
          $ref: "./ProjectApplicantItemsDto.yaml"
x-use:
  builder: true