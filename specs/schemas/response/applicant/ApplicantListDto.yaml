type: object
description: |
  応募者一覧
allOf:
  - $ref: ../MetaListResponse.yaml
  - type: object
    required:
      - items
    properties:
      items:
        type: array
        items:
          $ref: ./ApplicantItemsDto.yaml
x-use:
  builder: true
