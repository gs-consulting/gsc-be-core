type: object
required:
  - pageNumber
  - pageSize
  - arrangedBy
description: |
  ブラックリスト
properties:
  pageNumber:
    type: integer
    format: int32
    nullable: true
    description: Page number
    minimum: 1
  pageSize:
    type: integer
    format: int32
    nullable: true
    description: Number of items per page
    minimum: 1
  arrangedBy:
    type: string
    nullable: true
    description: |
      並び順: ASC or DESC
    example: "案件No.:ASC"