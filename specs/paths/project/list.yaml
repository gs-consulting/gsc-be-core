post:
  summary: Get all projects (案件一覧)
  security:
    - bearerAuth: []
  operationId: getProjectList
  description: |
    # Get all projects
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../schemas/request/project/ProjectSearchBoxDto.yaml"
  responses:
    200:
      description: Get all projects
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/project/ProjectListDto.yaml"
    401:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/ErrorResponse.yaml"
          example:
            message: Unauthorized
            statusCode: "401"
            fieldError: null
    403:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/ErrorResponse.yaml"
          example:
            message: Forbidden
            statusCode: "403"
            fieldError: null
    500:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/ErrorResponse.yaml"
          example:
            message: Internal server error
            statusCode: "500"
            fieldError: null
