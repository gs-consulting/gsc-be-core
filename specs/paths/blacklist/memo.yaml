put:
  summary: Edit a blacklist memo (選考対象外)
  security:
    - bearerAuth: []
  operationId: editBlacklistMemo
  description: |
    # Edit a blacklist memo
  parameters:
    - $ref: "../../parameters/path/id.yaml"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../schemas/request/blacklist/BlacklistMemoDto.yaml"
  responses:
    200:
      description: Save successfully
    401:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/ErrorResponse.yaml"
          example:
            message: Unauthorized
            statusCode: "401"
            fieldError: null
    403:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/ErrorResponse.yaml"
          example:
            message: Forbidden
            statusCode: "403"
            fieldError: null
    404:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/ErrorResponse.yaml"
          example:
            message: NotFound
            statusCode: "404"
            fieldError: null
    400:
      description: Bad Validation
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/ErrorResponse.yaml"
          example:
            message: Bad Request
            statusCode: "400"
            fieldError: null
    500:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: "../../schemas/response/ErrorResponse.yaml"
          example:
            message: Internal server error
            statusCode: "500"
            fieldError: null